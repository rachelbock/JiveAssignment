
CREATE DATABASE jive_challenge;

USE jive_challenge;

CREATE TABLE os_data (id INT NOT NULL AUTO_INCREMENT, os_name VARCHAR(100) NOT NULL, os_version VARCHAR(100) NOT NULL, notes TEXT, PRIMARY KEY (id));

